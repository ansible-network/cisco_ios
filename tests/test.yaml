#!/usr/bin/env ansible-playbook

---
- hosts: localhost
  vars:
    ansible_network_os: ios
  roles:
    - "{{ playbook_dir }}/../../ansible-network.network-engine"

  tasks:

#    - name: Include tests for `show ip bgp summary`
#      include_tasks: output/show_ip_bgp_summary/main.yaml
#
#    - name: Include tests for `show ip vrf detail`
#     include_tasks: output/show_ip_vrf_detail/main.yaml

    - name: Include tests for `show ip route vrf *`
      include_tasks: output/show_ip_route_vrf_all/main.yaml